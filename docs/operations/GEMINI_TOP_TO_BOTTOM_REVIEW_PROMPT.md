# Gemini / Antigravity — Top-to-Bottom MBT Platform Review Prompt

*Paste the block below into Antigravity (or any Gemini 2.5 Pro / 3.1 Pro agent with repo access). Review repo: `CPTV27/hillbilly-dreams`, main branch, as of commit `ef0f4d9` (2026-04-19).*

---

## THE PROMPT (paste verbatim)

```
You are doing a top-to-bottom review of the MBT (Measurably Better Things
LLC) platform — a multi-tenant Next.js 14 / Prisma / Sanity / Stripe monorepo
that powers a media-hospitality ecosystem anchored on the Mississippi
corridor with a Hudson Valley node.

In the last 48 hours, a partner agent (Claude Code running as "Cos") shipped
~55,000 lines across 42 commits. I want you to review all of it
independently — code, schema, docs, seed data, operational choices, security,
and business logic consistency — and flag everything questionable.

## Repo location

Clone: https://github.com/CPTV27/hillbilly-dreams
Branch: main
Pull HEAD at ef0f4d9 or later.

## What was built (orient yourself, then review)

1. SEVEN new module packages under `packages/modules/`:
   - @bigmuddy/commerce (Stripe subscription billing + one-off checkout)
   - @bigmuddy/booking (ticket inventory, quotes, bookings)
   - @bigmuddy/finance (Tier 3 engagement tracking, bucket hours, P&L)
   - @bigmuddy/events (coordination event bus — publish/subscribe)
   - @bigmuddy/broadcast (podcast, TikTok Live, clip extraction)
   - @bigmuddy/social (per-brand voice routing + cross-brand contamination guards)
   - @bigmuddy/content-creation (AI wizard for content generation)
   - @bigmuddy/email (Resend transactional templates)

2. 21 NEW PRISMA MODELS added to `packages/database/prisma/schema.prisma`,
   applied to production Neon Postgres as a single additive migration:
   Plan, Subscription, Product, Order, OrderItem, ModuleLicenseProfile,
   ModuleEngagement, RetainerHour, BusEvent, EventHandler, EventDelivery,
   PodcastShow, PodcastEpisode, LiveBroadcast, BroadcastClip, Resource,
   Booking, Ticket, QuoteRequest.

3. TWELVE SANITY SCHEMAS total (5 pre-existing + 7 new): article, location,
   event, touringPage, contentTemplate, podcastEpisode, magazineIssue,
   showEvent, staff, partnerArtist, pressRelease, faq.

4. 33+ API ROUTES under apps/web/app/api/{commerce,booking,finance,events,
   broadcast,podcast,wizard,webhooks,cron,account}.

5. NINE admin pages under apps/web/app/admin/ + a 4-step Content Creation
   Wizard at /admin/create/[contentType].

6. FIVE customer-facing public pages: /pricing, /shows, /private-events,
   /account/subscriptions, /checkout/{success,cancelled}.

7. 230+ DOCUMENTATION FILES across docs/{onboarding,voice,marketing-copy,
   video-scripts,customer-faqs,operator-guides,legal,api-reference,
   operations,partners,magazine-issues,hydration-guides,seed,plans}
   generated by a parallel Gemini batch dispatcher at
   scripts/ai/gemini-batch.mjs. Budget spent: ~$1.20, ~640K tokens.

8. 112 STRUCTURED SEED RECORDS in docs/seed/*.json targeting Sanity and
   Postgres, not yet imported to production.

9. DEPLOY VERIFICATION SCRIPT at scripts/verify-deploy.mjs reports 16/16
   green against https://bigmuddytouring.com.

10. TENANT PROVISIONING CLI at scripts/provision-tenant.mjs.

## What I want you to do

### Phase 1 — Read before judging

- Read `docs/BUSINESS_ARCHITECTURE.md` for business context
- Read `docs/THE_STORY.md` and `apps/web/app/story/page.tsx` for the narrative
- Read `docs/SOP_PARALLEL_BUILD.md` for how the work was executed
- Read the 7 module READMEs at `packages/modules/*/README.md`
- Read `packages/database/prisma/schema.prisma` — all 21 new models
- Skim `docs/voice/*.md` (brand voice docs — these constrain all content)

### Phase 2 — Critical review across these axes

**1. CODE QUALITY** (per module)
   - Error handling — are exceptions swallowed anywhere that should bubble?
   - Idempotency — are webhook handlers, event handlers, and API writes
     safe to retry? Are there race conditions?
   - Types — any `as never`, `as unknown`, or loose `any` that could
     silently break? Any places the Prisma types don't match the code's
     assumptions?
   - Security — SQL injection, XSS, tenant-isolation leaks, secret
     leakage, HMAC verification correctness?
   - Test coverage — there are ZERO tests; where's the highest-leverage
     place to add 10 tests and what would they cover?

**2. DATABASE SCHEMA**
   - Are the 21 new models normalized correctly? Any redundancies that
     should be foreign keys? Any missing indexes for the query patterns?
   - The Plan <-> Subscription relationship — is there a risk of orphan
     subscriptions if a Plan is deactivated?
   - The ModuleEngagement table links to Subscription by subscriptionId;
     is the null vs non-null semantic clear?
   - The RetainerHour table has no foreign-key into a Tenant/Entity table;
     is `consumingEntityId` free-string safe?
   - Are the cuid IDs consistent vs autoincrement int IDs (existing
     models use int)?

**3. MULTI-TENANT ISOLATION**
   - Middleware at apps/web/middleware.ts + config at
     apps/web/config/domain-routes.ts + config/tenants.ts — is tenant
     hostname resolution airtight? Can a request to one brand's domain
     ever render another brand's data?
   - The BusEvent.tenantId is a required field with a validation-against-
     tenant-config pattern in packages/modules/events/src/publish.ts —
     does this actually enforce isolation, or is it best-effort?
   - Tenant-scoped Prisma queries — are there any findMany calls that
     forget to filter by tenantId?

**4. BUSINESS LOGIC CONSISTENCY**
   - The Tier 3 platform-fee mechanism: Plan.platformFeePercent drives
     ModuleEngagement creation on subscription activation. If Plan is
     updated to change the fee %, existing Engagements keep the old %.
     Is this intentional or a bug?
   - The Booking module's "hold" timeout — Booking.heldUntil + the cron
     sweep in expireOldHolds(). What happens if the cron misses a run?
     Capacity reserved forever?
   - The wizard generates drafts to Sanity with drafts.<ulid> prefix.
     Is there any path where the wizard publishes directly (violating
     the Tracy-promotes-from-drafts rule)?
   - Voice-router cross-brand contamination guards throw on
     incompatible voice profiles. Are these guards in the RIGHT places,
     or can code bypass them via direct publisher bypass?

**5. DOCUMENTATION ACCURACY**
   - Are the API reference docs actually correct vs the exported
     functions? (Gemini drafted these in batch 2.)
   - Are the brand voice docs internally consistent — e.g., Magazine
     claims to target Inn guests, but does any marketing copy actually
     treat Magazine as music-industry?
   - The onboarding guides (for Tracy, Amy, Vicki, etc.) — do they
     accurately reflect what those people will actually see/do on Day 1?
   - Legal drafts (docs/legal/*_DRAFT.md) — flag the 5 most likely
     issues a lawyer would catch.

**6. SEED DATA QUALITY**
   - 112 seed records in docs/seed/*.json — are they internally
     consistent (e.g., do podcast episodes reference valid show ids)?
   - Are the Plan prices actually aligned with docs/marketing-copy/
     pricing-page-copy-*.md and the docs/marketing-copy/*-tiers.md?
   - Are the FAQ entries appropriate for their declared brand voice?

**7. OPERATIONAL READINESS**
   - Cron jobs — /api/events/worker (30s), /api/cron/podcast-sync
     (hourly). Do these actually exist in vercel.json, or is that a gap?
   - The Mac mini broadcast agent at scripts/broadcast-agent/agent.mjs —
     all the instruction handlers are STUBBED. Flag this explicitly.
   - Deploy verification at scripts/verify-deploy.mjs covers 16 URLs.
     What's NOT covered that should be?
   - Env var reference at docs/operations/ENVIRONMENT_VARIABLES.md —
     cross-reference with actual code to find any env var reads that
     aren't documented.

**8. MISSING PIECES**
   - What important functionality was promised in the plans but not
     actually built? (Check docs/plans/*.md vs actual code.)
   - What was built but not wired (orphaned code)?

### Phase 3 — Deliver the review

Write a comprehensive review document with this structure:

# MBT Platform — Independent Review

## Executive summary
(3-5 sentences. Is the platform in shippable shape? Blocking issues? Soft concerns?)

## Top 10 findings (ranked by severity)
For each:
- **Severity** (CRITICAL / HIGH / MEDIUM / LOW / COSMETIC)
- **Category** (security / schema / code / docs / business logic / operational)
- **File reference** (`path/to/file.ts:line`)
- **What's wrong**
- **Why it matters**
- **Recommended fix**
- **Estimated effort** (hours / days)

## Per-axis findings
(Detailed findings per axis 1-8 above. Use the same Severity / Category /
File / Description / Fix / Effort structure for each.)

## What was done well
(Acknowledge the genuinely good decisions. Keep honest — no boosterism.)

## What would I do differently
(Your recommendations if you were starting over with the same constraints.)

## Approval matrix
For each major subsystem, state: SHIP / FIX FIRST / BLOCK. Subsystems:
Commerce, Booking, Finance, Events, Broadcast, Social, Content Creation,
Email, Admin UI, Customer Pages, Docs, Seed Data, Deploy + Migration,
Tenant Provisioning, Security Model.

## Open questions
(Things you couldn't answer from the repo alone that a human needs to
confirm. Enumerate.)

Be honest. Be specific. File + line references are non-negotiable.
If you flag something as CRITICAL, back it with a reproduction path.
Skip positive review of things that are just OK — focus on actionable
deltas. Aim for 5,000-8,000 words.
```

---

## Usage

1. **Copy the prompt block above** (everything between the triple-backtick code fences).
2. **Open Antigravity** (or your preferred Gemini 2.5 Pro / 3.1 Pro agent with Git tooling).
3. **Paste as the initial turn.** Ensure Gemini has repo access (MCP git server, or upload via Google Drive).
4. **Give it ~30-60 min** of real-time processing (6 axes × 8 concerns × dozens of files = real work).
5. **Expect output** in the 5,000-8,000 word range. Save to `docs/operations/REVIEW_BY_GEMINI_<date>.md`.

## What to do with the findings

- **CRITICAL / HIGH** → file as issues in GitHub; block next deploy until resolved.
- **MEDIUM** → add to `docs/router/QUEUE.md` as P-review projects.
- **LOW / COSMETIC** → batch into a cleanup sprint when convenient.
- **Open questions** → answer in a follow-up doc or Slack thread, then commit as context files.

## Budget expectation

Gemini 3.1 Pro at ~$1.25/M in, $5/M out. A thorough 30-minute review across a ~55K-line codebase + 230 docs:
- Input: probably 400K-800K tokens (lots of file reading)
- Output: 10K-20K tokens
- Cost estimate: **$0.75 - $1.50**

Cheap insurance against a surprise in prod.
