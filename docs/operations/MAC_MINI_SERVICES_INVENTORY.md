<!--
  Generated by scripts/ai/gemini-batch.mjs on 2026-04-19T13:05:12.905Z
  Task: tech-doc-mac-mini-services-inventory
  Model: gemini-2.5-flash
  Tokens: 7510 (prompt 514 + completion 2990)
-->

This document provides a comprehensive inventory of services running on the Mac mini identified by IP address `192.168.4.37`, operating under the `ClawdBOT` user account. It details each service's function, access points, credentials, operational procedures, and recovery strategies, serving as a critical reference for system administration and maintenance.

---

## Mac mini Services Inventory: `192.168.4.37` (ClawdBOT)

### Overview

This Mac mini (IP: `192.168.4.37`) serves as a central hub for various automation, streaming, and content management tasks, primarily supporting the "Big Muddy Radio" operations and associated workflows. All services listed below are configured to run under the `ClawdBOT` user account, ensuring proper resource isolation and permissions.

### General Mac mini Information

#### SSH Access

Secure Shell (SSH) access to the Mac mini is established using a specific SSH key. This is the primary method for remote administration and troubleshooting.

*   **Command:** `ssh -i ~/.ssh/id_mini ClawdBOT@192.168.4.37`
*   **Key Location:** The private SSH key `id_mini` is expected to be located in the `~/.ssh/` directory of the connecting machine.
*   **User:** `ClawdBOT` is the designated user for all service operations and administrative tasks on this machine.

#### Network Requirements

The Mac mini has specific network dependencies crucial for its operations:

*   **Local Area Network (LAN) Connectivity:**
    *   **Purpose:** Essential for internal communication between services and for client access from the local network (e.g., a MacBook).
    *   **Affected Services:** OpenBroadcaster, Icecast, Plex, Postiz, Open Notebook all rely on being accessible from within the LAN. The `broadcast-agent` also interacts with OBS Studio locally, which requires stable internal communication.
    *   **Dependency:** The Mac mini must be connected to the same LAN as any client machine (e.g., a MacBook) intending to access its services via their respective internal IP addresses and ports.
*   **Internet Connectivity:**
    *   **Purpose:** Required for services that interact with external APIs, cloud platforms, or require updates.
    *   **Affected Services:**
        *   **`broadcast-agent`:** Polls the MBT server (external) and interacts with TikTok Live.
        *   **Postiz:** Connects to various social media platforms for scheduling.
        *   **OpenBroadcaster:** May fetch external metadata, news feeds, or updates.
        *   **Plex:** Requires internet for metadata fetching, remote access, and account authentication.
        *   **Icecast:** While primarily a local stream server, may depend on internet for external source clients or administrative features.
    *   **Dependency:** A stable and consistent internet connection is mandatory for the full functionality of these services. Services with cron-driven tasks (like `broadcast-agent` polling) are particularly sensitive to internet availability during their execution windows.

#### Boot-time Persistence (`launchd`)

All services are configured to start automatically upon system boot (or user login for `LaunchAgents`) and to remain running. This is managed by macOS's `launchd` system. For services running under the `ClawdBOT` user, their configuration files (PLISTs) are typically located in `~/Library/LaunchAgents/`.

A typical `launchd` plist for a user agent looks like this:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.example.service</string> <!-- Unique identifier for the service -->
    <key>ProgramArguments</key>
    <array>
        <string>/path/to/executable</string> <!-- The actual program to run -->
        <string>--arg1</string>             <!-- Any arguments for the program -->
    </array>
    <key>WorkingDirectory</key>
    <string>/path/to/service/root/</string> <!-- Directory where the program should run from -->
    <key>RunAtLoad</key>
    <true/> <!-- Ensures the service starts when the agent is loaded -->
    <key>KeepAlive</key>
    <true/> <!-- Restarts the service if it crashes or exits unexpectedly -->
    <key>StandardOutPath</key>
    <string>/path/to/logs/stdout.log</string> <!-- Redirects standard output -->
    <key>StandardErrorPath</key>
    <string>/path/to/logs/stderr.log</string> <!-- Redirects standard error -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string> <!-- Example PATH -->
    </dict>
</dict>
</plist>
```

When restarting services, `launchctl` commands are used to unload and load these plist files.

#### Backup Strategy (General)

A multi-layered backup strategy is employed to protect service data and configurations:

1.  **Time Machine:** The entire Mac mini's system is backed up using Apple's Time Machine to an external drive or network-attached storage. This provides a comprehensive system-level recovery point. Critical service data directories should be explicitly included in Time Machine's scope (i.e., not excluded).
2.  **`rsync` for Critical Data:** For specific, highly critical application data, `rsync` scripts are scheduled (e.g., via `cron` or another `launchd` agent) to regularly synchronize data directories to a secure network share or a separate external drive. This allows for granular, frequent backups of dynamic content.
3.  **Configuration Files:** All `launchd` plists and application-specific configuration files (`.conf`, `.xml`, `.env`, etc.) are considered critical and are included in both Time Machine and `rsync` backups.
4.  **Frequency & Retention:**
    *   **Time Machine:** Hourly backups, retaining historical versions as space permits.
    *   **`rsync`:** Daily or more frequent, depending on the service's data volatility. Retention policies for `rsync` backups are typically 7-30 days of daily snapshots.
5.  **Restoration Notes:**
    *   Always attempt to restore to a test environment first, if possible.
    *   Verify data integrity after restoration.
    *   Be mindful of file permissions and ownership when restoring data, especially for `ClawdBOT` user services.

---

### Service Inventory

#### 1. OpenBroadcaster

*   **What it does:** A web-based platform primarily used for scheduling and automating radio broadcasts.
*   **Access:**
    *   **URL:** `http://192.168.4.37:8080`
    *   **Port:** `8080` (TCP)
*   **Credentials:**
    *   **Bitwarden Item:** `OpenBroadcaster ClawdBOT`
    *   **Details:** Contains the administrative username and password for the web interface.
*   **Persistence (`launchd`):**
    *   **Plist Path:** `~/Library/LaunchAgents/com.openbroadcaster.scheduler.plist`
    *   **Label:** `com.openbroadcaster.scheduler`
    *   **Notes:** This service is likely a Java application or similar, launched via a shell script wrapper defined in the `ProgramArguments` of its plist.
*   **Restart Procedure:**
    1.  **SSH** into the Mac mini: `ssh -i ~/.ssh/id_mini ClawdBOT@192.168.4.37`
    2.  **Unload and Load `launchd` agent:**
        ```bash
        launchctl unload ~/Library/LaunchAgents/com.openbroadcaster.scheduler.plist
        # Wait a few seconds for the process to terminate gracefully
        launchctl load ~/Library/LaunchAgents/com.openbroadcaster.scheduler.plist
        ```
    3.  **Verification:** Check health after restart.
*   **Health Check:**
    1.  **Process Check:** `ps aux | grep -i "[o]penbroadcaster"` (look for the main process, often Java).
    2.  **Port Listen Check:** `lsof -i :8080` (should show a process listening on 8080).
    3.  **HTTP Status Check:** `curl -sI http://192.168.4.37:8080 | head -n 1` (expected output: `HTTP/1.1 200 OK`).
    4.  **Web Interface:** Navigate to `http://192.168.4.37:8080` in a browser and ensure the login page loads.
*   **Logs Location:**
    *   `~/Library/Logs/OpenBroadcaster/` (common application log location)
    *   `~/Library/LaunchAgents/com.openbroadcaster.scheduler.plist.stdout.log` (for `stdout`)
    *   `~/Library/LaunchAgents/com.openbroadcaster.scheduler.plist.stderr.log` (for `stderr`)
    *   Check the `WorkingDirectory` specified in the plist for service-specific logs.
*   **Backup Strategy:**
    *   **Critical Data:** Database files (e.g., SQLite, PostgreSQL data directory), configuration files (e.g., `openbroadcaster.conf`, `application.properties`), media scheduling data.
    *   **Location:** Typically within the OpenBroadcaster installation directory or a designated data folder (`~/OpenBroadcaster/data/`, `~/OpenBroadcaster/config/`).
    *   **Method:** `rsync` the entire data directory to a network share daily. Time Machine will also cover these files.
    *   **Frequency:** Daily `rsync`, hourly Time Machine.
*   **Common Failures & Fixes:**
    *   **Problem:** Service fails to start or becomes unresponsive (often Java-related).
        *   **Fix:** Check `stdout.log` and `stderr.log` for Java exceptions or out-of-memory errors. Increase Java heap size if memory is an issue (configured in the `ProgramArguments` of the plist). Restart the service.
    *   **Problem:** Database connection errors.
        *   **Fix:** Verify database service (if external) is running and accessible. Check OpenBroadcaster's configuration files for correct database credentials and connection strings.
    *   **Problem:** Port 8080 already in use.
        *   **Fix:** Use `lsof -i :8080` to identify the conflicting process and terminate it, or change OpenBroadcaster's configured port (requires updating configuration and `launchd` plist if URL is hardcoded).

#### 2. Icecast

*   **What it does:** An open-source streaming media server, specifically used for broadcasting audio streams for Big Muddy Radio.
*   **Access:**
    *   **URL:** `http://192.168.4.37:8010` (for status page)
    *   **Port:** `8010` (TCP)
*   **Credentials:**
    *   **Bitwarden Item:** `Icecast Admin`
    *   **Details:** Contains administrative username/password for the web interface and source passwords for stream clients.
*   **Persistence (`launchd`):**
    *   **Plist Path:** `~/Library/LaunchAgents/homebrew.mxcl.icecast.plist` (typical for Homebrew services)
    *   **Label:** `homebrew.mxcl.icecast`
    *   **Notes:** If installed via Homebrew, `brew services` manages the plist.
*   **Restart Procedure:**
    1.  **SSH** into the Mac mini.
    2.  **Using `brew services` (recommended):**
        ```bash
        brew services restart icecast
        ```
    3.  **Using `launchctl` (if `brew services` is not used or fails):**
        ```bash
        launchctl unload ~/Library/LaunchAgents/homebrew.mxcl.icecast.plist
        # Wait a few seconds
        launchctl load ~/Library/LaunchAgents/homebrew.mxcl.icecast.plist
        ```
    4.  **Verification:** Check health after restart.
*   **Health Check:**
    1.  **Process Check:** `ps aux | grep -i "[i]cecast"` (should show `icecast` process).
    2.  **Port Listen Check:** `lsof -i :8010` (should show `icecast` listening).
    3.  **HTTP Status Check:** `curl -sI http://192.168.4.37:8010 | head -n 1` (expected output: `HTTP/1.0 200 OK`).
    4.  **Admin Page:** Access `http://192.168.4.37:8010/status.