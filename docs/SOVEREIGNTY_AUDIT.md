# Multi-Tenant Sovereignty Audit — 2026-04-05

Generated by Rook (Patch v3). Covers all API routes in `apps/web/app/api/`.

## Summary

| Category | Count | % |
|----------|-------|---|
| Protected (requireAdmin / cron / session) | 63 | 50% |
| Unprotected (no auth) | 64+ | 50% |
| Webhook-verified (Stripe sig / HMAC) | 4 | — |

**Half the API surface has no authentication.** This is the critical finding.

---

## High-Risk Unprotected Routes

### AI/ML Endpoints (expensive, no auth)
- `POST /api/ai/analyze` — Vertex AI Gemini 3.1 Pro
- `POST /api/ai/notebook/chat` ��� AI chat with Prisma context
- `POST /api/content/transcribe` ��� OpenAI transcription
- `POST /api/media/audio` — Cloud TTS voice generation
- `POST /api/media/enhance` — Image enhancement
- `POST /api/media/video` — Video generation

**Risk:** Anyone can call these and run up Google Cloud / OpenAI bills.

### Financial Operations (no auth)
- `GET/POST /api/billing` — Invoice list and creation
- `POST /api/billing/subscribe` — Creates Stripe checkout sessions
- `POST /api/connect/onboard` — Starts Stripe Connect onboarding
- `GET /api/connect/status` — Checks Connect status
- `POST /api/stripe/checkout` — Creates checkout sessions
- `POST /api/stripe/onboard` — Starts onboarding

**Risk:** Unauthenticated Stripe session creation.

### Cron Routes (no auth in prod)
- `POST /api/cron/scan-asana` — No CRON_SECRET check
- `POST /api/cron/dynamic-pricing` — No CRON_SECRET check
- `POST /api/cron/sync-qbo` — No CRON_SECRET check
- `GET /api/cron/sync-google` — Has CRON_SECRET check (good)

**Risk:** External callers can trigger scheduled tasks.

### Content Mutation (no auth)
- `POST /api/articles` — Creates articles
- `POST /api/drafts/generate` — Generates multi-channel drafts
- `POST /api/drafts/refine` — Refines content
- `PUT /api/drafts` — Updates pending drafts
- `POST /api/content/schedule` — Schedules social posts
- `POST /api/embeddings/index` — Triggers vector indexing
- `POST /api/marketing/scout` — Scout endpoint
- `POST /api/page-edits` — Page edit CRUD
- `POST /api/notebook/drop` — Notebook drop creation

**Risk:** Unauthenticated content creation and modification.

### Client/Business Data (no auth on reads)
- `GET /api/clients/[id]/route` — Returns client details
- `GET /api/clients/[id]/reviews` — Lists reviews
- `GET /api/clients/[id]/calendar` — Content calendars
- `POST /api/directory/claim` — Claim a business listing
- `POST /api/directory/enrich` — Enrich from Google Places
- `POST /api/directory/submit` — Submit new business

**Risk:** Client data exposed without session check.

---

## Protected Routes (correctly guarded)

### requireAdmin()
All admin CRUD, agent endpoints, editorial bureau, tasks API, GBP audit, review response, Studio C pipeline, marketing DNA/reviews/campaigns.

### requireCronOrAdmin()
Report generation, report PDF, artists API, census sync, social publish batch.

### Session-based (auth())
Client list, ambient endpoints, Google/QBO ingestion.

### Webhook signature verification
Stripe webhooks (billing, connect), bridge ingest (HMAC-SHA256).

---

## Intentionally Public Routes

These should remain open:
- `GET /api/atom` — RSS/Atom feed
- `GET /api/feed` — Public feed
- `GET /api/search` — Public search
- `GET /api/search/instant` — Instant search
- `GET /api/directory/map-points` — Public map pins
- `GET /api/directory/[slug]` — Public business pages
- `GET /api/tracks` — Public track listing
- `GET /api/articles/[id]` — Public article reads
- `GET /api/public/knowledge` — Public knowledge base
- `POST /api/directory/submit` — Public business submission (intentional)
- `POST /api/artists/onboard` — Public artist signup
- `POST /api/user/onboarding` — Public user onboarding

---

## Fix Priority

### P0 — Fix This Week
1. Add `requireAdmin()` to all `/api/ai/*` routes
2. Add CRON_SECRET bearer to `/api/cron/scan-asana`, `/api/cron/dynamic-pricing`, `/api/cron/sync-qbo`
3. Add `requireAdmin()` to `/api/billing` POST and `/api/stripe/checkout` POST

### P1 — Fix This Month
4. Add `requireAdmin()` to all `/api/media/*` mutation routes (audio, enhance, video, upload)
5. Add session check to `/api/clients/[id]` GET routes (or scope to owner)
6. Add `requireAdmin()` to `/api/drafts/*`, `/api/content/schedule`, `/api/page-edits`
7. Add `requireAdmin()` to `/api/productions/*` mutation routes

### P2 — Review and Decide
8. Review `/api/directory/claim` and `/api/directory/enrich` — are these intentionally public?
9. Review `/api/gchat/*` — should these have a bot verification token?
10. Review `/api/ops/*` read routes — are these internal-only?

---

## Multi-Tenant Query Filtering

**Not audited in this pass.** Group-scoped queries (ensuring Client A can't read Client B's data) require reviewing every Prisma query for `clientId` or `tenantId` filtering. This is a separate audit task.

---

*Generated: 2026-04-05 by Rook (Patch v3)*
