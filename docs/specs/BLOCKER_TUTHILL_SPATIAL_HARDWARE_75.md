# Blocker: Tuthill spatial scanning product (#75)

**Status:** **BLOCKED — hardware + calibration**

## Product intent (spec exists in roadmap)

Gaussian splat / dense mesh → floor plans → MLS-safe deliverables for Tuthill listings.

## Blockers

1. **Camera / LiDAR kit** — inventory which devices Elijah+Miles own vs need purchase.
2. **Workstation GPU** — splat processing machine spec (Mac Studio class or PC with CUDA).
3. **Matterport subscription** — active account + transfer to Tuthill billing if required.

## Spec placeholder

Full workflow doc ships when hardware list is fixed — reference `TUTHILL_ZILLOW_PROVIDER.md` for deliverable packages.

## Unblock

- [ ] Hardware signed off + insured for on-site work.
