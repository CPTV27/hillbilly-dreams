<!--
  Generated by scripts/ai/gemini-batch.mjs on 2026-04-19T17:25:27.497Z
  Task: groq-queries-amy
  Model: gemini-2.5-flash
  Tokens: 5055 (prompt 59 + completion 1974)
-->

Amy is a content manager, event organizer, and digital strategist, likely working with a dynamic content platform like Sanity.io. Her role involves managing podcasts, live events, partnerships, articles, and sponsor relationships. To effectively navigate and extract critical information from her content repository, Amy relies heavily on GROQ (Graph-Relational Object Queries) – Sanity's powerful query language.

These 10 GROQ queries are tailored to Amy's specific operational needs, demonstrating how she leverages GROQ to gain insights, identify gaps, and streamline her workflows. They assume a content schema designed to support her diverse responsibilities.

### Amy's Content Schema Overview

Before diving into the queries, let's outline the core document types Amy works with:

*   **`podcastEpisode`**: Represents a single podcast episode with fields like `title`, `slug`, `description`, `audioFile`, `publishedAt`, and an array of `guests` (references to `person`).
*   **`show`**: Details about an event or performance, including `title`, `slug`, `date`, `venue`, `artists` (references to `artist`), `genre`, and `description`.
*   **`artist`**: Information about performing artists, with `name`, `slug`, `bio`, `tiers` (an array of strings like "Platinum", "Gold"), `website`, and `socialLinks`.
*   **`article`**: Blog posts or news articles, containing `title`, `slug`, `body` (portable text), `publishedAt`, `tags` (references to `tag`), and `author` (reference to `person`).
*   **`tag`**: Simple document for categorizing articles, with `name` and `slug`.
*   **`sponsor`**: Details about sponsoring entities, including `name`, `slug`, `logo`, and `tier`.
*   **`plan`**: Represents a specific sponsorship package or agreement, with `name`, `amount`, `duration`, and a `sponsor` reference.
*   **`liveStream`**: Information about upcoming or past live streams, including `title`, `platform` (e.g., "TikTok", "YouTube"), `scheduledFor`, `description`, `presenter` (reference to `person`), and `platformId`.
*   **`person`**: A general document for individuals, used for podcast guests, article authors, and live stream presenters, with `name`, `bio`, and `image`.

---

### Amy's Top-10 GROQ Queries

Here are Amy's go-to GROQ queries, each serving a distinct purpose in her daily operations:

---

#### 1. Podcast Episodes Missing Description

**Query Goal:** Identify podcast episodes that lack a description, indicating incomplete content that needs attention before publication or for SEO optimization.

**GROQ Query:**

```groq
*[_type == "podcastEpisode" && !defined(description)] | order(publishedAt desc) {
  title,
  "slug": slug.current,
  publishedAt,
  _id
}
```

**Schema Context:** This query targets the `podcastEpisode` document type and specifically checks the `description` field.

**Explanation:**
The query starts by filtering for all documents where `_type` is `podcastEpisode`. The core of this query lies in the condition `!defined(description)`. The `defined()` function in GROQ checks if a field exists and has a value. By negating it with `!`, Amy can pinpoint episodes where the `description` field is either entirely missing or explicitly set to `null`. The results are then ordered by `publishedAt` in descending order, showing the most recent incomplete episodes first. Finally, it projects only the `title`, the `current` value of the `slug` (aliased as "slug"), the `publishedAt` timestamp, and the document `_id` for easy reference.

**Example Use Case:** Amy uses this query as a quick audit tool. She runs it regularly to catch any podcast episodes that were published or are scheduled for publication without a proper description, ensuring all content meets quality standards and is discoverable.

---

#### 2. Upcoming Blues Room Shows

**Query Goal:** Retrieve a list of all future shows categorized under the "Blues" genre, helping Amy manage event schedules and promote upcoming performances.

**GROQ Query:**

```groq
*[_type == "show" && genre == "Blues" && date > now()] | order(date asc) {
  title,
  date,
  venue,
  "artists": artists[]->name,
  "slug": slug.current
}
```

**Schema Context:** This query focuses on the `show` document type, specifically utilizing the `genre` and `date` fields, and references to `artist` documents.

**Explanation:**
This query begins by selecting all documents of `_type == "show"`. It then applies two crucial filters: `genre == "Blues"` to narrow down to specific musical events, and `date > now()` to only include shows scheduled for the future. The `now()` function dynamically provides the current timestamp, making the query always relevant. The results are ordered chronologically with `date asc`. The projection includes the `title`, `date`, `venue`, and a nested projection for `artists`. `artists[]->name` dereferences each artist reference within the array to fetch only their `name`, providing a clean list of performers for each show. The `slug.current` is also included for URL generation.

**Example Use Case:** Amy uses this query to populate the "Upcoming Blues Shows" section on her website, generate promotional materials, or quickly check the schedule for artist liaison work.

---

#### 3. Partner Artists by Tier (e.g., "Platinum")

**Query Goal:** Find all artists who are designated as "Platinum" partners, allowing Amy to identify top-tier collaborators for special promotions or exclusive events.

**GROQ Query:**

```groq
*[_type == "artist" && "Platinum" in tiers] | order(name asc) {
  name,
  website,
  socialLinks,
  bio
}
```

**Schema Context:** This query targets the `artist` document type and specifically checks the `tiers` array field.

**Explanation:**
The query filters for documents where `_type` is `artist`. The condition `"Platinum" in tiers` is a powerful GROQ feature that checks if the string "Platinum" is present within the `tiers` array for any given artist. This allows for flexible categorization where an artist might belong to multiple tiers. The results are then sorted alphabetically by the `name` field in ascending order. The projection retrieves the artist's `name`, `website`, `socialLinks`, and `bio`, providing a comprehensive overview of these key partners.

**Example Use Case:** Amy frequently runs this query when planning high-profile events or campaigns, needing to quickly access contact information and profiles of her most valued artist partners. She might also adapt it to find "Gold" or "Silver" tier artists.

---

#### 4. Recent Radio-Tagged Articles

**Query Goal:** Discover articles published within the last 30 days that are specifically tagged with "Radio," useful for tracking media mentions or relevant industry news.

**GROQ Query:**

```groq
*[_type == "article" && publishedAt > (now() - 60 * 60 * 24 * 30) && references(*[_type == "tag" && name == "Radio"]._id)] {
  title,
  "slug": slug.current,
  publishedAt,
  author->name
}
```

**Schema Context:** This query involves `article` documents, their `publishedAt` field, and references to `tag` documents.

**Explanation:**
This query starts by selecting `article` documents. It applies two primary filters. First, `publishedAt > (now() - 60 * 60 * 24 * 30)` calculates a timestamp 30 days ago from the current moment (`now()`) and filters for articles published more recently. The `60 * 60 * 24 * 30` represents 30 days in seconds. Second, `references(*[_type == "tag" && name == "Radio"]._id)` is a powerful sub-query. It first finds the `_id` of the `tag` document whose `name` is "Radio", and then the outer query filters `article`s that `references` that specific tag ID. The projection includes the `title`, `slug.current` (aliased as "slug"), `publishedAt` date, and the `name` of the `author` (by dereferencing the `author` reference).

**Example Use Case:** Amy uses this to monitor recent news related to her radio segments or to quickly find content for social media promotion that aligns with her radio initiatives.

---

#### 5. Sponsor Log (from Plans) - All Plans with their Sponsors

**Query Goal:** Get a comprehensive list of all sponsorship plans, detailing the plan's attributes and the associated sponsor's information, crucial for financial tracking and partner management.

**GROQ Query:**

```gro